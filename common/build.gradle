plugins {
    id 'java-library'
}
base { archivesName = 'flowchat-common' }
tasks.withType(JavaCompile).configureEach { options.release = 21 }
dependencies {
    api 'com.google.code.gson:gson:2.11.0'
    api 'org.slf4j:slf4j-api:2.0.16'
}
